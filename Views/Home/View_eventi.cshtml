@model List<string>
@{
    ViewBag.Title = "Calendario - Mattia Semprini";
}

<h2>Eventi in Programma:</h2>
@using (Html.BeginForm("Calendario", "Home")){
<div class="container">
    <br />
    <div class="row">
        <div class="span5">
            
            
                <input type="button" class="btn btn-primary active" value="aggiorna">

            
            
            <table class="table table-striped table-condensed">
                <thead>
                    <tr>
                        <th>Lista Eventi:</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var x in Model)
                    {
                    <tr>
                        <td>
                            @x
                        </td>
                    </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>
</div>
}

